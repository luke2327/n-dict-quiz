import type { NextPage } from 'next'
import Head from 'next/head'
import styles from '../styles/Home.module.css'
import { Box, Typography } from '@mui/material';
import LanguageChooser from '@components/general/LanguageChooser';

const Home: NextPage = () => {
  return (
    <div className={styles.container}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Box maxHeight={400}>
        <Typography variant='h1'>Naver dictonary Quiz</Typography>
        <Typography variant='subtitle2'>alpine</Typography>
        <hr></hr>
        <Box display="flex" justifyContent={'space-between'}>
          <Typography variant="subtitle1">1.0.0</Typography>
          <Typography variant="caption">2022-05-30</Typography>
        </Box>
      </Box>
      <Box pt={4}>
        <Typography variant='h2'>Choose language</Typography>
        <LanguageChooser />
      </Box>
    </div>
  )
}

export default Home
